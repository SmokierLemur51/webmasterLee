[tool.poetry]
name = "core"
version = "0.1.0"
description = ""
authors = ["SmokierLemur51 <69629938+SmokierLemur51@users.noreply.github.com>"]
readme = "README.md"
packages = [{include = "core", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
flask = "^3.0.3"
flask-sqlalchemy = "^3.1.1"
flask-login = "^0.6.3"
flask-bcrypt = "^1.0.1"
python-dotenv = "^1.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
start = "core:run"